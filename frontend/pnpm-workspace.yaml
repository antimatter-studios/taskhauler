# pnpm 11+ reads project-wide settings from this file. We're not actually a
# workspace — this is just where `onlyBuiltDependencies` lives now (it used
# to be in package.json's `pnpm` field, which pnpm 11 ignores).
onlyBuiltDependencies:
  - esbuild
  - "@tailwindcss/oxide"
